# Profile — who the AI is working with

The "about me" the assistant reads at session start, so it doesn't have to ask who you are, what you own, or how you like to work. Keep it short and current — this is context, not a CV.

> Replace everything below with your own. The entries are placeholders showing the shape.

## Who I am
- **<Your role, e.g. Backend engineer>** on **<team / product>**. I own **<the part of the system you're responsible for>** — that's the perspective I reason from by default.
- I occasionally cross into **<adjacent areas / repos>** when a feature spans them; treat that as task-driven, not my main lane.

## Stack I work in
- **<Language / framework>** — e.g. Java / Spring Boot
- **<Datastore / migrations>** — e.g. PostgreSQL / Flyway
- **<Testing>** — e.g. JUnit, Spock
- **<Anything else worth knowing>** — tooling, infra, conventions

## Working style
- **<How you like reviews / PRs handled>** — e.g. per-thread replies over bulk comments.
- **<Answer style>** — e.g. concise answers and direct trade-offs over walls of options.
- **<Default framing lens>** — e.g. "what does my service own vs. what belongs elsewhere."

## Team & stakeholders
- **<Who you work with>** — direct teammates, reporting line, cross-team partners. Link out to a separate note if this gets long.

## Domain conventions
- **<Units / formats your work assumes>** — currency, date format, naming conventions, default doc classification.
- **<House rules>** — anything the AI should apply consistently in outputs.

## Guardrails — needs sign-off / escalation
List the topics where the AI should **stop and flag for human sign-off** rather than producing a final answer unilaterally. Tailor to your domain. Examples:
- **<Sensitive decision logic>** — e.g. anything affecting money movement, access control, or compliance rules.
- **<Regulated / legal areas>** — e.g. privacy-sensitive data flows.
- **<Incidents>** — customer-facing outages, data or security incidents → route to your incident process, don't draft external comms solo.
